import AuthPage from '../componets/complex/auth-page';
import { Layout } from '../componets/containers/layout';

const Auth = () => {
  return (
    <Layout>
      <AuthPage />
    </Layout>
  );
};

export default Auth;
